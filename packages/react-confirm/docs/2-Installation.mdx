# Installation

To install the package, run:

```bash
npm install @react-confirm/react-confirm
```

```bash
yarn add @react-confirm/react-confirm
```

```bash
pnpm add @react-confirm/react-confirm
```
